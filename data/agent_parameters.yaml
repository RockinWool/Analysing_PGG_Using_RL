# Define number of repeat
{
    # Q-networkの学習係数
    "learning_rate":0.0001,
    # バッファーメモリの大きさ
    "memory_size" :1000,
    # Q-networkを更新するバッチのサイズ
    "batch_size":100,
    # 割引係数 default 0.99
    "gamma":0.9,
}
